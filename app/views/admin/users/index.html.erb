<h1>Listing users</h1>

<table>
  <tr>
    <th>id</th>
    <th>Username</th>
    <th>Nom</th>
    <th>Prénom</th>
  </tr>
<% @users.each do |user| %>
  <tr>
    <td><%= user.id %></td>
    <td><%= user.username %></td>
    <td><%= user.nom %></td>
    <td><%= user.prenom %></td>
  </tr>
<% end %>
</table>